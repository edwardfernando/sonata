<table class="table table-hover table-bordered">
  <tr>
    <td>Name</td>
    <td>#</td>
  </tr>

  <% @people.each do |p| %>
  <tr>
    <td><%= p.name %></td>
    <td>
      <a href="javascript: " onclick="choose('<%= params[:name] %>', '<%= params[:target] %>', '<%= p.id %>', '<%= p.name %>');" class="btn btn-default btn-xs">
        <span class="glyphicon glyphicon-import"></span> Choose
      </a>
    </td>
  </tr>
  <% end %>
</table>
