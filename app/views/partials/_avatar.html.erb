<% if person.avatar_url.blank? %>
  <img src="<%= image_path("default_avatar.png") %>" width="<%=width%>" height="<%=height%>" class="<%=css%>"/>
<% else %>
  <%= cl_image_tag(person.avatar_url, :width => width, :height => height, :crop => :fill, :class => css) %>
<% end %>
